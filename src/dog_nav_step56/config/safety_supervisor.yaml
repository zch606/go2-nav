/**:
  ros__parameters:
    imu_topic: /imu/data
    odom_topic: /odometry/filtered
    cmd_vel_topic: /cmd_vel
    terrain_cost_topic: /terrain_cost
    safety_topic: /safety_state
    publish_rate_hz: 10.0
    sensor_timeout_sec: 1.5
    nominal_speed_limit: 0.6
